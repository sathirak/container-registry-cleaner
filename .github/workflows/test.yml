name: Test My Go Action

on: [push, 'workflow_dispatch']

jobs:
  test-job:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: ./ # local path to your action
        with:
          name: 'Go Developer'
